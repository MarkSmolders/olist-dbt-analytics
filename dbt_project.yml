name: 'olist_analytics'
version: '1.0.0'
config-version: 2

profile: 'olist-dbt-bigquery'

model-paths: ["models"]

target-path: "target"
clean-targets:
  - "target"

models:
  olist_analytics:
    staging:
      +materialized: view
    marts:
      core:
        +materialized: table
      analytics:
        +materialized: table